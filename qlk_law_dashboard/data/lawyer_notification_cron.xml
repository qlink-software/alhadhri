<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate="1">
    <record id="ir_cron_daily_lawyer_reminder" model="ir.cron">
        <field name="name">Daily Lawyer Reminder</field>
        <field name="model_id" ref="model_qlk_lawyer_notification"/>
        <field name="state">code</field>
        <field name="code">model.cron_daily_lawyer_reminder()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="nextcall"
               eval="(DateTime.now().replace(hour=5, minute=0, second=0, microsecond=0) + relativedelta(days=1)).strftime('%Y-%m-%d %H:%M:%S')"/>
        <field name="active" eval="True"/>
    </record>
</odoo>
